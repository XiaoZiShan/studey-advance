package studey.advance.basearithmetic.sort;

import studey.advance.datastructure.pojo.MbRelatedWeightResource;

import java.util.List;

/**
 * Created by 使用 lambda 语法进行元素比较排序
 */
public class LambdaSortRecursion{

    // 模拟排序
    public List<MbRelatedWeightResource> sort(List<MbRelatedWeightResource> t){
        return null; //t.stream().sorted(e - > e.get).collect(Collectors.toList());
    }

    // 进行倒序
    
    // 组合排序
    
    // 兼容 Comparable 与 Comparator

}
